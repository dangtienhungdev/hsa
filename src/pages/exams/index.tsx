

const ExamPage = () => {
  // const { data } = useQuery({
  //   queryKey: ['exams'],
  //   queryFn: () => examApi.getExams(),
  // });

  // console.log(data);

  return <div>ExamPage</div>;
};

export default ExamPage;
